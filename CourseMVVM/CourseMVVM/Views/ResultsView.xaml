<Window x:Class="CourseMVVM.Views.ResultsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BrainC#" Height="318.188" Width="294.728" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <ListBox HorizontalAlignment="Left" Height="214" Margin="10,10,0,0" VerticalAlignment="Top" Width="255" ItemsSource="{Binding List}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Number}"/>
                        <TextBlock Text="{Binding Category}"/>
                        <TextBlock Text="{Binding TotalPoints}"/>
                        <TextBlock Text="{Binding Points}"/>
                        <TextBlock Text="{Binding Mark}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Закрыть" HorizontalAlignment="Left" Margin="68,239,0,0" VerticalAlignment="Top" Width="146" Height="34" Command="{Binding CloseCommand}"/>

    </Grid>
</Window>
