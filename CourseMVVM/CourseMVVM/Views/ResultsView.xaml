<Window x:Class="CourseMVVM.Views.ResultsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ResultsView" Height="324.188" Width="311.728">
    <Grid>
        <ListBox HorizontalAlignment="Left" Height="224" Margin="10,10,0,0" VerticalAlignment="Top" Width="284" ItemsSource="{Binding List}">
            <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                    <TextBlock Text="{Binding Number}"/>
                    <TextBlock Text="{Binding Category}"/>
                    <TextBlock Text="{Binding TotalPoints}"/>
                    <TextBlock Text="{Binding Points}"/>
                    <TextBlock Text="{Binding Mark}"/>
                </StackPanel>
            </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Close" HorizontalAlignment="Left" Margin="53,239,0,0" VerticalAlignment="Top" Width="204" Height="40" Command="{Binding CloseCommand}"/>

    </Grid>
</Window>
